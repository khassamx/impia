import os
from openai import OpenAI
import json

# Asegúrate de que la variable de entorno HF_TOKEN esté configurada en tu sistema.
# Si no lo está, la línea os.environ["HF_TOKEN"] fallará.
try:
    HF_TOKEN = os.environ["HF_TOKEN"]
except KeyError:
    print("🛑 ERROR: La variable de entorno HF_TOKEN no está configurada.")
    print("Necesitas tu token de Hugging Face para usar este código.")
    exit()


# 1. Configurar el cliente OpenAI para Hugging Face Router
client = OpenAI(
    # La base_url es el router unificado de Hugging Face
    base_url="https://router.huggingface.co/v1",
    api_key=HF_TOKEN,
)

# 2. Realizar la solicitud de completado de chat
try:
    print("⏳ Solicitando diálogo al modelo moonshotai/Kimi-K2-Instruct-0905 (Proveedor: Groq)...")
    completion = client.chat.completions.create(
        model="moonshotai/Kimi-K2-Instruct-0905",
        messages=[
            {
                "role": "user",
                "content": "Write a dialogue between two characters meeting for the first time."
            }
        ],
        # Puedes añadir otros parámetros como max_tokens, temperature, etc.
    )

    # 3. Imprimir el mensaje generado por el asistente
    dialogue = completion.choices[0].message.content
    print("\n" + "="*50)
    print("✅ Diálogo Generado:")
    print("="*50)
    print(dialogue)
    print("="*50)

except Exception as e:
    print(f"\n🚨 Ocurrió un error al llamar a la API: {e}")
    print("Verifica si tu HF_TOKEN es válido o si tienes saldo de inferencia.")
